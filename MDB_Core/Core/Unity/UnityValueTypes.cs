// Unity Value Types and Common Structs
// These are minimal stubs for Unity's built-in value types
// They allow the generated SDK to compile

using System;

namespace UnityEngine
{
    // Note: Most Unity value types (Vector2, Vector3, Color, etc.) are now auto-generated from dump.cs
    // Only keep stubs here for types that need special handling or aren't in the dump
}

// System.IO types that may be referenced
namespace System.IO
{
    // FileInfo is part of .NET but may need explicit reference
}
